import { redirect, notFound } from 'next/navigation';
import { AdminShell } from '@/components/admin/shell';
import { getSessionFromCookies } from '@/lib/auth/session';
import { listAppointments } from '@/lib/storage';
import { confirmationMessage } from '@/lib/notifications';
export const dynamic='force-dynamic';
function maskCpf(cpf?:string|null){return cpf?cpf.replace(/^(\d{3})\d+(\d{2})$/,'$1.***.***-$2'):'Não informado'}
export default async function AppointmentDetails({params}:{params:{id:string}}){const session=getSessionFromCookies(); if(!session) redirect('/login'); const a=(await listAppointments(session.companyId)).find((item:any)=>item.id===params.id); if(!a) notFound(); const date=new Date(`${a.date}T${a.startTime}:00.000Z`); const msg=confirmationMessage({company:a.source,consultant:a.consultantId,date,address:a.address,protocol:a.protocol}); return <AdminShell title="Detalhes do agendamento"><div className="grid gap-4 lg:grid-cols-[1fr_360px]"><section className="rounded-2xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900"><h2 className="text-xl font-black">{a.clientName}</h2><p className="text-sm text-zinc-500">{a.clientPhone} · {a.clientEmail??'sem e-mail'} · CPF {maskCpf(a.clientCpf)}</p><div className="mt-4 grid gap-2 text-sm"><p><strong>Data:</strong> {a.date} {a.startTime}</p><p><strong>Endereço:</strong> {a.address}</p><p><strong>Origem:</strong> {a.source}</p><p><strong>Status:</strong> {a.status}</p><p><strong>Protocolo:</strong> {a.protocol}</p></div></section><aside className="rounded-2xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900"><h3 className="font-black">Mensagem</h3><pre className="mt-3 whitespace-pre-wrap rounded-xl bg-zinc-100 p-3 text-xs dark:bg-zinc-800">{msg}</pre></aside></div></AdminShell>}
