'use client';

import { useEffect, useMemo, useState, type FormEvent } from 'react';
import { CheckCircle2, Copy, MessageCircle, QrCode } from 'lucide-react';

type PublicLink = { id: string; slug: string; duration: number; availableDays: number[]; availableSlots: string[]; reservedSlots: string[]; appearance?: { buttonColor?: string | null } };
type Confirmation = { protocol: string; mensagem: string };
function isoFor(date: string, slot: string) { return `${date}T${slot}:00.000Z`; }
function dateKey(offset: number) { const date = new Date(); date.setUTCHours(0,0,0,0); date.setUTCDate(date.getUTCDate()+offset); return date.toISOString().slice(0,10); }

export function PublicBookingForm({ link }: { link: PublicLink }) {
  const days = useMemo(() => Array.from({ length: 30 }, (_, index) => index).filter((offset) => { const date = new Date(`${dateKey(offset)}T00:00:00.000Z`); return link.availableDays.includes(date.getUTCDay()); }).slice(0, 14), [link.availableDays]);
  const [day, setDay] = useState(days[0] ?? 0);
  const [slots, setSlots] = useState<string[]>([]);
  const [slot, setSlot] = useState('');
  const [loadingSlots, setLoadingSlots] = useState(false);
  const [result, setResult] = useState<Confirmation | null>(null);
  const [error, setError] = useState<string | null>(null);
  const selectedDate = dateKey(day);

  useEffect(() => { setLoadingSlots(true); setSlot(''); fetch(`/api/public/booking-links/${link.slug}/slots?date=${selectedDate}`).then((r) => r.json()).then((json) => { const next = json.data?.slots ?? []; setSlots(next); setSlot(next[0] ?? ''); }).catch(() => setSlots([])).finally(() => setLoadingSlots(false)); }, [link.slug, selectedDate]);

  async function submit(event: FormEvent<HTMLFormElement>) {
    event.preventDefault(); if (!slot) return setError('Escolha um horário disponível.');
    const formData = new FormData(event.currentTarget); setError(null); setResult(null);
    const response = await fetch('/api/public/appointments', { method: 'POST', headers: { 'content-type': 'application/json' }, body: JSON.stringify({ slug: link.slug, startsAt: isoFor(selectedDate, slot), client: { fullName: formData.get('fullName'), phone: formData.get('phone'), email: formData.get('email') || undefined, cpf: formData.get('cpf') || undefined }, notes: formData.get('notes') || undefined }) });
    const json = await response.json(); if (!response.ok) return setError(json.error ?? 'Não foi possível agendar.'); setResult({ protocol: json.data.protocol, mensagem: json.data.mensagem });
  }
  if (result) return <div className="rounded-3xl border border-green-200 bg-white p-6 shadow-soft dark:border-green-900 dark:bg-zinc-900"><h2 className="text-2xl font-black text-green-700">Visita confirmada</h2><p className="mt-2 text-sm text-zinc-500">Protocolo {result.protocol}</p><pre className="mt-4 whitespace-pre-wrap rounded-2xl bg-zinc-100 p-4 text-sm dark:bg-zinc-800">{result.mensagem}</pre><div className="mt-4 flex flex-wrap gap-2"><button onClick={() => navigator.clipboard.writeText(result.mensagem)} className="inline-flex items-center gap-2 rounded-xl border border-zinc-200 px-4 py-3 font-bold dark:border-zinc-800"><Copy size={18}/> Copiar</button><a target="_blank" className="inline-flex items-center gap-2 rounded-xl bg-atto-green px-4 py-3 font-bold text-white" href={`https://wa.me/?text=${encodeURIComponent(result.mensagem)}`}><MessageCircle size={18}/> WhatsApp</a></div></div>;
  return <form onSubmit={submit} className="rounded-3xl border border-zinc-200 bg-white p-6 shadow-soft dark:border-zinc-800 dark:bg-zinc-900"><h2 className="text-2xl font-black">Escolha o melhor horário</h2><div className="mt-5 grid gap-6 lg:grid-cols-2"><div><p className="mb-3 font-bold">1. Data</p><div className="grid grid-cols-2 gap-2 sm:grid-cols-3">{days.map((offset) => { const d = new Date(`${dateKey(offset)}T00:00:00.000Z`); return <button type="button" key={offset} onClick={() => setDay(offset)} className={`rounded-xl border p-3 text-left ${day === offset ? 'border-atto-red bg-atto-red text-white' : 'border-zinc-200 dark:border-zinc-800'}`}><span className="block text-sm opacity-70">{d.toLocaleDateString('pt-BR', { weekday: 'short', timeZone: 'UTC' })}</span><strong>{d.toLocaleDateString('pt-BR', { day: '2-digit', month: '2-digit', timeZone: 'UTC' })}</strong></button>; })}</div></div><div><p className="mb-3 font-bold">2. Horário</p>{loadingSlots ? <p className="rounded-xl border border-zinc-200 p-4 text-sm text-zinc-500 dark:border-zinc-800">Carregando horários...</p> : slots.length ? <div className="grid grid-cols-2 gap-2">{slots.map((value) => <button type="button" key={value} onClick={() => setSlot(value)} className={`rounded-xl border px-4 py-3 font-semibold ${slot === value ? 'border-atto-red bg-atto-red text-white' : 'border-zinc-200 dark:border-zinc-800'}`}>{value}</button>)}</div> : <p className="rounded-xl border border-zinc-200 p-4 text-sm text-zinc-500 dark:border-zinc-800">Sem horários disponíveis nesta data.</p>}</div></div><h2 className="mt-8 text-2xl font-black">Seus dados</h2><div className="mt-5 grid gap-4 md:grid-cols-2"><Input name="fullName" label="Nome completo" placeholder="Maria Silva" required/><Input name="phone" label="Telefone" placeholder="(11) 99999-9999" required/><Input name="email" label="E-mail (opcional)" placeholder="maria@email.com"/><Input name="cpf" label="CPF (opcional)" placeholder="000.000.000-00"/><label className="md:col-span-2"><span className="text-sm font-semibold">Observações</span><textarea name="notes" className="mt-2 min-h-24 w-full rounded-xl border border-zinc-200 bg-transparent p-3 outline-none focus:border-atto-red dark:border-zinc-800" placeholder="Conte alguma preferência para o atendimento"/></label></div>{error && <p className="mt-4 rounded-xl border border-red-200 bg-red-50 p-3 text-sm text-red-700">{error}</p>}<div className="mt-6 flex flex-col gap-3 rounded-2xl border border-zinc-200 p-4 dark:border-zinc-800 md:flex-row md:items-center md:justify-between"><div><p className="font-bold">Resumo</p><p className="text-sm text-zinc-500">{selectedDate} às {slot || '--:--'} · {link.duration} min</p></div><div className="flex gap-2"><button type="button" className="inline-flex items-center gap-2 rounded-xl border border-zinc-200 px-4 py-3 font-bold dark:border-zinc-800"><QrCode size={18}/> QR Code</button><button style={{ background: link.appearance?.buttonColor ?? undefined }} className="inline-flex items-center gap-2 rounded-xl bg-atto-red px-5 py-3 font-bold text-white"><CheckCircle2 size={18}/> Confirmar</button></div></div></form>;
}
function Input({ name, label, placeholder, required }: { name: string; label: string; placeholder: string; required?: boolean }) { return <label><span className="text-sm font-semibold">{label}</span><input name={name} required={required} className="mt-2 w-full rounded-xl border border-zinc-200 bg-transparent p-3 outline-none focus:border-atto-red dark:border-zinc-800" placeholder={placeholder}/></label>; }
